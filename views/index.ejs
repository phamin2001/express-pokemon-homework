<!DOCTYPE html>
<html lang="en">
    <head>
    <link rel="stylesheet" type="text/css" href="css/style.css" />
 
    </head>
    <body>
        <ul class="pokemon">
            <%  pokemon.forEach(function(eachPokemon, index) { %>
                    <li><%=JSON.stringify(eachPokemon)%></li>
            <% }) %>
        </ul>
           

    </body>
</html>